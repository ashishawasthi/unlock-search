# Core runtime (the local profile runs with just these).
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9
pypdf>=4.0
httpx>=0.27
pyyaml>=6.0
# Per-target extras live in requirements-onprem.txt and requirements-gcp.txt.
